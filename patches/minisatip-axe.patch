diff --git a/src/stream.c b/src/stream.c
index d2c1226..5697ca1 100644
--- a/src/stream.c
+++ b/src/stream.c
@@ -994,7 +994,6 @@ int process_dmx(sockets *s)
 #endif
 
 	rlen = ad->rlen;
-	check_cc(ad);
 
 	for (i = 0; i < MAX_STREAMS; i++)
 		if (st[i] && st[i]->enabled && st[i]->adapter == ad->id)
